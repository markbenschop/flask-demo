---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: flask-demo-ingress
  namespace: demo
spec:
  rules:
      - host: mark.omen.intern
        http:
          paths:
          - backend:
              serviceName: flask-demo-service
              servicePort: 80
            path: /flask-demo

